# CSV更新への協力

一般利用者はGitHub Pull Requestではなく、公開するGoogleフォームへ取得直後のCSVを提出してください。GitHubアカウントは不要ですが、bot対策と差分照合のためGoogleへのログインが必要です。

- ファイル名や内容を加工せず提出してください。
- 1アカウントの同じ情報だけでは更新されません。別の確認済みGoogleアカウントから同じ譜面差分が届くと自動検証PRになります。
- CSV全体が一致する必要はありません。確認済みの譜面差分から順に反映します。
- 譜面追加・削除・理論値・定数・初出バージョン変更は自動反映しません。

保守者がCSVを手動でPRへ追加する場合は、`data/updates/` に新規CSVを1件だけ追加し、同じPRでコード・メタデータ・既存CSVを変更しないでください。
